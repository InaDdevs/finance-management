name: projeto01
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.3 # Banco de dados SQLite
  path: ^1.9.0 # Para encontrar o caminho do banco de dados
  intl: ^0.19.0 # Para formatter de datas e moeda
  provider: ^6.1.2 # Para gerenciamento de estado (opcional, mas recomendado)
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0


flutter:

  uses-material-design: true

